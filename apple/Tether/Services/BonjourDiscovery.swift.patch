--- Tether/Services/BonjourDiscovery.swift
+++ Tether/Services/BonjourDiscovery.swift
@@ -97,6 +97,8 @@
             let fingerprint = extractFingerprint(from: result.metadata)
             
+            print("BONJOUR: discovered name=\(name) fp=\(fingerprint) local=\(self.localFingerprint ?? "none")")
+
             if let local = self.localFingerprint, !local.isEmpty, fingerprint == local {
                 return nil
             }
